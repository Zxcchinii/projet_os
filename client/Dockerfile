# File: client/Dockerfile
FROM nginx:alpine

# Copy the HTML and any additional static files
COPY index.html /usr/share/nginx/html/

# Optional: Custom nginx configuration if needed
# COPY nginx.conf /etc/nginx/conf.d/default.conf

# Expose HTTP port
EXPOSE 80